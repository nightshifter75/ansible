---
- name: Set configuration files
  hosts: all

  tasks:

    - name: .bashrc lines
      blockinfile:
        path: /home/parallels/.bashrc
        block: "{{ lookup('file', 'block_bashrc') }}"
        state: present
        backup: yes

    - name: .bash_profile lines
      block:
        - name: Add lines to an existing .bash_profile file
          blockinfile:
            path: /home/parallels/.bash_profile
            block: "{{ lookup('file', 'block_bash_profile') }}"
            state: present
            backup: yes
      rescue:
        - name: If .bash_profile does not exists, I create one
          copy:
            src: block_bash_profile
            dest: /home/parallels/.bash_profile
      always:
        - name: Say something!
          debug:
            msg: "Profile created"

    - name: .vimrc file
      copy:
        src: block_vimrc
        dest: /home/parallels/.vimrc
        backup: yes
... 
